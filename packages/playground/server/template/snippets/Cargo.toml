# This excludes the template from the rest of the workspaces.
[workspace]

[package]
name = "play-{BUILD_ID}"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { version = "0.7.0-rc.1", features = ["web", "router"] }
dioxus-primitives = { git = "https://github.com/DioxusLabs/components", rev = "3571d90", version = "0.0.1", default-features = false, features = ["router"] }
time = { version = "0.3.44", features = ["std", "macros", "wasm-bindgen"] }
tracing = "0.1.41"
wasm-bindgen = "=0.2.100"
web-sys = { version = "0.3", features = ["Location"] }

[profile.dev.package."*"]
opt-level = 'z'
debug = false
strip = true

[profile.dev.package.dioxus-devtools]
opt-level = 'z'
debug = true
strip = true

[profile.dev.package.dioxus-web]
opt-level = 'z'
debug = true
strip = true
